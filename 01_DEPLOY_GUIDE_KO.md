# 화랑 WORKSPACE 12단계 테스트 서버 적용 안내

이 ZIP은 재감사를 거친 11단계 수정본 전체에 배포 안내와 파일 검증 목록을 추가한 **테스트 서버용 배포 후보**입니다. 일부 수정 파일만 담은 패치가 아닙니다. 앱 실행 코드와 계산식은 11단계 재감사본과 동일합니다.

## 1. 가장 먼저 할 일

- [ ] VS Code에서 테스트 저장소 `insurance_tools_Test`를 열었는지 확인합니다.
- [ ] 기존 테스트 프로젝트를 다른 폴더에 통째로 복사하거나, 되돌릴 수 있는 Git 커밋을 남깁니다.
- [ ] 운영 저장소와 혼동하지 않도록 폴더명과 원격 저장소를 확인합니다.
- [ ] ZIP을 별도 임시 폴더에 압축 해제합니다. ZIP 파일 자체를 서버 실행 파일로 올리지 않습니다.

VS Code 터미널에서 확인할 수 있는 명령입니다. 파일을 수정하거나 전송하지 않습니다.

```powershell
git remote -v
git branch --show-current
git status --short
```

## 2. 어디에 무엇을 넣나요?

압축을 풀면 바로 `app.py`가 나옵니다. 이 파일이 기존 테스트 프로젝트의 `app.py`와 같은 위치에 오도록 반영합니다. 프로젝트 안에 새 폴더를 하나 더 만들어 그 안에 전부 넣는 방식은 피합니다.

|파일·폴더|역할|반영 방법|
|---|---|---|
|app.py|앱 실행 시작점|같은 위치에 반영|
|modules/|모든 업무·공통 화면 코드|폴더 안 파일 전체 반영|
|assets/|글꼴·로고|전체 반영|
|data/|공식 링크·교육·문구·양식 JSON|전체 반영|
|requirements.txt|설치할 패키지 목록|constraints.txt와 반드시 함께 반영|
|constraints.txt|검사에 사용한 의존성 48개 버전|requirements.txt 옆에 반영|
|.streamlit/config.toml|화면 테마·앱 설정|반영. 압축 해제 시 누락되지 않았는지 확인|
|tests/|자동 검사 코드|전체 보관|
|docs/|기준 기록·재감사 증거|전체 보관|
|README.md, 01_DEPLOY_GUIDE_KO.md|현재 적용 안내|반영|
|STAGE12_RELEASE_NOTES.md|이번 구성·검증 범위|반영|
|RELEASE_MANIFEST.json|파일별 SHA-256 목록|보관|
|이전 단계 *.md|개발 이력|참고용으로 보관|

다음 항목은 ZIP에 포함하지 않습니다. 기존 것을 유지합니다.

- `.streamlit/secrets.toml` 또는 서버에 설정된 `passwords` 값.
- 기존 저장소의 `.git` 폴더.
- 개인 PC의 가상환경 폴더.

ZIP의 `.gitignore`는 일반적인 로컬 설정/캐시 제외 규칙입니다. 기존 프로젝트에도 `.gitignore`가 있으면 기존 사용자 규칙을 버리지 말고 필요한 항목을 병합하세요.

직원 명단과 기존 보너스 매칭은 사용자 결정대로 유지했습니다. 별도 직원 설정 파일을 추가로 작성할 필요는 없습니다.

## 3. VS Code에서 로컬 실행을 먼저 확인하는 경우

검사에 사용한 Python은 3.12입니다. 다른 버전에서의 설치 성공을 이번 작업에서 확인한 것은 아닙니다. 아래는 Windows PowerShell 기준이며, 프로젝트 루트에서 실행합니다.

이미 사용하는 정상 가상환경이 있다면 그 Python 경로로 설치·실행해도 됩니다. 새 환경을 만들려면 기존 환경과 겹치지 않는 이름을 사용합니다.

```powershell
py -3.12 -m venv .venv_hwarang_stage12
.\.venv_hwarang_stage12\Scripts\python.exe -m pip install -r requirements.txt
.\.venv_hwarang_stage12\Scripts\python.exe -m unittest discover -s tests
.\.venv_hwarang_stage12\Scripts\python.exe -m streamlit run app.py
```

가상환경 활성화 스크립트 없이 Python을 직접 호출하므로 PowerShell 실행 정책을 변경할 필요가 없습니다. `py -3.12`가 없다는 메시지가 나오면 현재 선택된 Python 버전을 확인하세요. 설치가 실패했는데 다음 단계의 테스트 성공으로 기록하면 안 됩니다.

## 4. 테스트 서버에 반영

- [ ] 위 파일을 테스트 Git 저장소에 반영합니다.
- [ ] VS Code 변경 목록에서 대상 파일을 확인합니다. ZIP·가상환경·로그·개인 설정이 변경 목록에 섞이지 않게 확인합니다.
- [ ] 확인한 파일만 커밋하여 현재 테스트 앱이 연결한 브랜치에 푸시합니다. 이 안내는 대신 커밋하거나 푸시하지 않습니다.
- [ ] 테스트 앱의 저장소·브랜치·실행 파일 경로가 의도한 값인지 확인합니다. 기본 실행 파일은 `app.py`입니다.
- [ ] 서버의 기존 로그인 설정은 그대로 둡니다.
- [ ] 의존성 설치·앱 재실행 결과를 확인합니다. `requirements.txt`와 `constraints.txt` 중 하나라도 빠지면 설치가 실패할 수 있습니다.

새 환경 설치는 이번 실행 환경에서 PyPI 접속이 차단되어 완료하지 못했습니다. 따라서 실제 서버 설치 로그 확인이 반드시 남아 있습니다. 특정 패키지를 찾지 못하거나 버전 충돌이 나면 임의로 고정값을 지우기보다 오류에 나온 패키지와 Python 버전을 기준으로 확인해야 합니다.

## 5. 적용 후 10분 확인표

가상 입력으로 먼저 확인합니다. 항목에 체크하는 것은 실제로 실행한 경우에 한합니다.

- [ ] 기존 비밀번호로 로그인할 수 있습니다.
- [ ] 홈의 원수사 검색에 `현대`를 입력하면 결과가 나오고 검색 초기화가 됩니다.
- [ ] 각 역할에서 허용된 메뉴만 보입니다.
- [ ] 상담·비교표·고객자료 화면의 불필요한 개인정보 안내가 제거된 상태입니다.
- [ ] 보장분석 원본의 상품명을 문자 `=2+2`로 저장해 변환하면 결과 Excel에도 같은 문자로 보입니다. 합계 수식과 월보험료는 정상입니다.
- [ ] 리모델링에서 한글 상품명과 `=2+2` 같은 문자열을 넣어 출력할 수 있습니다.
- [ ] 정상 XLSX는 읽고, 손상된 파일은 안내와 함께 거절합니다.
- [ ] 고객자료의 검토 확인 후 다운로드가 가능하고, 내용을 바꾸면 다시 검토가 필요합니다.
- [ ] 원수사 포털의 연락처·공식자료 팝업이 열립니다.
- [ ] PC와 모바일에서 제목·입력·팝업·다운로드 버튼이 잘리지 않습니다.
- [ ] 기존 주요 계산기의 평소 사용 예시와 결과를 비교합니다.

주의할 구분: 이번 12단계는 포장·안내 단계라 홈이나 화면을 새로 디자인하지 않았습니다. 기존 업무 화면의 ‘10단계’ 안내가 남아 있어도 코드 누락을 뜻하지 않습니다. 배포한 커밋과 `STAGE12_RELEASE_NOTES.md`, 위 기능 동작으로 적용 여부를 판단하세요.

## 6. 문제가 생겼을 때 되돌리기

1. 문제가 발생한 화면과 오류 코드, 입력 조건을 기록합니다.
2. 테스트 저장소를 적용 전 백업/커밋 상태로 되돌립니다. 다른 사람의 작업이 있는 브랜치에서 강제 푸시를 사용하지 않습니다.
3. 앱 코드뿐 아니라 `requirements.txt`, `constraints.txt`, 데이터 파일도 같은 버전으로 되돌립니다.
4. 이전 버전에 constraints 파일이 없었다면 새 파일만 남겨 혼합하지 않습니다.
5. 테스트 앱을 다시 실행하고 로그인·홈 검색·주요 도구를 확인합니다.

로그인 비밀 설정은 이 과정에서 새 값으로 덮어쓰지 않습니다. 운영 서버에는 본 파일을 자동 적용하지 않습니다.

## 7. 현재 검증 범위

- 11단계 수정 후 전체 자동 검사: 104개 통과, 84.918초.
- 상품명 문자 출력·의도된 합계 수식·금액 보존 재현 통과.
- 가짜 XML XLSX 차단, 100,000/100,001행 경계, 암호화 PDF 거절 확인.
- 12단계는 앱 코드가 동일하므로 전체 테스트를 반복 실행하지 않았으며, 런타임 파일 바이트 동일성과 ZIP/파일 목록을 검사합니다.

실제 새 서버 설치, PC·모바일 브라우저와 출력물 육안 검사, 최신 보험·세법 기준 전수 대조, 실제 다중 사용자 부하는 미검증입니다. **12단계 파일 구성 완료와 최종 운영 출시 승인 완료는 서로 다릅니다.**
