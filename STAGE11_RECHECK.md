# 11단계 수정 후 재감사

2026-09-23 / 10단계 전체 앱을 기반으로 출시감사에서 발견한 누락과 개선 사항을 수정했습니다.

## 판정

R-01 출력 누락과 R-02 재현된 가짜 XLSX 통과 문제는 수정 후 재현 검사에서 해소됐습니다. R-03 설치 버전 관리는 보완했으나 새 환경 설치 완료는 미검증입니다. 따라서 **확인된 코드 결함 수정본으로 테스트 서버 적용 가능**, 최종 운영 출시의 전체 검증 완료를 의미하지는 않습니다.

직원 명단·보너스 매칭 유지와 개인정보 관련 화면 안내 제거는 기존 사용자 결정대로 유지합니다. 실제 서버에 배포하지 않았습니다.

## R-01: 보장분석 상품명 출력 수정

`modules/analyzer.py`에서 회사명과 마찬가지로 상품명도 명시적인 문자 셀로 출력하도록 수정했습니다.

출력 재현 결과:

- `보장 분석!D3`: 값 `=2+2`, 타입 s(문자).
- `보장 분석!A7`: 값 `=SUM(D7:D7)`, 타입 f(의도된 수식 유지).
- `보장 분석!D7`: 월보험료 100000 유지.

새 회귀 테스트는 `=2+2`, `+SUM(1,2)`, `@SUM(1,2)`, `-2+2`, 일반 한글 상품명을 회사명·상품명·기간·납입횟수·납입주기에 넣어 문자 타입을 검사합니다. 합계 수식·금액·보장금액·시트 구성·인쇄영역도 함께 확인합니다. 의도된 업무 계산식은 변경하지 않았습니다.

## R-02: XLSX 구조 검사 보완

신규 `modules/xlsx_structure.py`를 `validators.py`에 연결했습니다.

- content-types와 통합문서·관계 파일의 XML 루트/네임스페이스 확인.
- 지원하는 XLSX 통합문서 MIME 형식 확인.
- 최상위 문서 관계가 실제 통합문서를 가리키는지 확인.
- 시트 관계 ID, 내부 연결 대상 존재, 워크시트 형식 확인.
- 외부 시트를 가리키는 관계와 잘못된 경로 거절.
- 실제 연결된 워크시트 XML의 루트와 기존 행·셀 한도 검사.
- 중복 내부 파일과 중복 관계 ID 거절.

필수 파일명에 `<dummy/>`만 넣은 이전 재현 파일은 이제 1차 검사에서 거절됩니다. 정상 XLSX, 누락된 시트, 외부 시트 연결, 잘못된 XML 루트와 MIME 형식을 검사하는 테스트를 추가했습니다. 모든 OOXML 문법을 검증하는 완전한 XSD 검사나 모든 종류의 Excel 파일 지원을 뜻하지 않습니다. XLSX 일반 워크시트 기반 업무 자료가 대상입니다.

추가 경계 검사: 100,000행 허용 / 100,001행 거절 / 암호화 PDF 거절을 다시 확인했습니다.

## R-03: 설치 버전 관리

현재 검사 환경에서 사용한 직접·전이 의존성 48개 버전을 `constraints.txt`에 고정했습니다. `requirements.txt`의 `-c constraints.txt`로 연결하여 함께 적용되도록 했습니다. 기존에 추가된 pypdf도 고정 대상입니다.

Python 3.12 / Linux 환경에서 해석한 의존성 조합입니다. Windows 전용 조건부 의존성까지 동일하게 고정한 목록은 아닙니다. 새 가상환경을 만들고 설치를 시도했지만 실행 환경의 네트워크 정책이 `https://pypi.org:443` 접속을 차단하여 설치 완료를 확인하지 못했습니다. 이를 통과로 기록하지 않았습니다.

실제 배포 환경에서 다음 명령으로 새 설치와 검사를 확인해야 합니다.

```text
python -m pip install -r requirements.txt
python -m unittest discover -s tests
streamlit run app.py
```

버전 고정은 버전이 자동으로 달라지는 문제를 줄이며, 패키지 공급처의 가용성·보안 검증이나 새 환경 설치 성공을 보증하지 않습니다.

## 반영 방법

1. 기존 테스트 저장소를 백업합니다.
2. ZIP 전체를 같은 프로젝트 위치에 반영합니다.
3. 특히 `modules/xlsx_structure.py`, `requirements.txt`, `constraints.txt`를 누락하지 마세요.
4. 의존성을 반영하고 테스트 서버를 재시작합니다.
5. 보장분석 정상 자료와 문자 상품명 출력, 정상 XLSX·손상 XLSX 처리를 확인합니다.

기존 화면 레이아웃은 유지하므로 화면 전체가 새 디자인으로 바뀌는 작업본은 아닙니다. 롤백은 이전 10단계 파일과 requirements로 되돌립니다.

## 남은 출시 검증

- 새 환경의 의존성 설치와 실제 서버 기동.
- PC·모바일 실제 브라우저의 입력·팝업·다운로드·가독성.
- PDF/Excel 실제 앱에서의 표시·인쇄 육안 검사.
- 최신 세법·실손·청구 기준 및 모든 기존 업무 경계의 전수 대조.
- 실제 호스팅의 다중 사용자·최대 입력 부하.

위 항목은 이번 코드 재검사와 구분하며 자동 완료로 표시하지 않습니다. ZIP의 `docs/recheck`에는 가상 자료 재현 결과를 포함했습니다.

## 이번 실행 결과

전체 자동 검사 104개 통과(84.918초). 기존 96개에 재감사 회귀 8개를 추가했습니다. 가상 출력 재현 및 100,000/100,001행·암호화 PDF 검사를 별도로 실행했습니다. 직원 명단 AST도 10단계와 동일합니다.
