# 화랑 WORKSPACE 테스트 서버 — 홈 개편 1차

작성일: 2026-09-21

이 묶음은 제공된 테스트 서버 파일의 수정본입니다. 실제 호스팅 서버에 업로드하거나 본 서버를 변경하지 않았습니다. 파일 도구함은 포함하지 않습니다.

## 반영한 내용

- 누락된 Pretendard 글꼴, 보험사 로고 27개 및 출처 목록, Streamlit 화면 설정 복원.
- 새 홈: 업무 목적별 빠른 시작 6개, 업무 도구 검색, 6개 분야별 기존 도구 12개 연결, 테스트 환경 표시.
- 새 사이드바: 분야별 탐색, 현재 도구 강조, 계정 표시, 업데이트 안내, 로그아웃.
- 홈 반응형 규칙: 본문 16px, 보조문구 최소 14px, 넉넉한 버튼 높이, 768px 이하 홈 카드 1열, 키보드 포커스, 동작 축소 설정 지원.
- Streamlit 사용 통계 전송 비활성화. 고객 DB·유료 서비스·새 외부 API는 추가하지 않음.
- 보장분석: 기존 본 서버의 엑셀 서식 보완을 동기화(제목 14pt, 제안서 보험연령, 나눔고딕, 보험사명/입력영역 글자색 등).
- 썸머: 기존 본 서버의 Excel 기록 안전 변환을 동기화(pd.NA, NaT, NaN, NumPy 스칼라).

## 오류 예방

| 예상 문제 | 반영한 대응 |
| --- | --- |
| 위젯 키 충돌 | 신규 홈·사이드바에 별도 키 접두사 적용, 반복 이동 테스트 |
| 버튼까지 카드 CSS가 적용됨 | 카드 컨테이너와 실행 버튼 키 접두사 분리 |
| 권한 없는 페이지 접근 | 버튼 비활성화, 이동 함수 권한 재검사, 잘못된 경로는 홈 복귀 |
| 로그인 설정 누락 | 화면 전체 오류 대신 설정 확인 안내, 로그인 차단 |
| 빈 비밀번호·등록되지 않은 계정 | 인증 실패 처리, 기존 5개 권한 유지 |
| 모바일 카드 넘침 | 홈 범위의 1열 레이아웃 및 여백 조정 |
| Excel 결측값 기록 오류 | 안전한 빈 셀/기본 숫자형으로 변환 |
| 계산식 변경에 따른 회귀 | 기존 모듈 AST 비교: 322개 기존 함수 동일, 변경은 엑셀 출력 함수 범위 |
| 입력값 손실 | 화면 이동·새로고침 전 결과 다운로드 안내. 모든 도구의 입력 유지 기능은 이번 범위에서 구현하지 않음 |

## 검증

환경: Python 3.12 / Streamlit 1.61.0 / 제공된 requirements.txt.

- Python 구문 컴파일 통과.
- 로컬 테스트 서버 기동 및 `/_stcore/health` HTTP 200 확인.
- 자동 테스트 11개 통과. 하위 시나리오에 기존 12개 도구 초기 렌더링·홈 복귀, 5개 권한의 홈, 로그인/로그아웃, 잘못된 로그인, 도구 검색/결과 없음, 권한 차단/잘못된 경로, 반복 이동, 복원 자산, 썸머 결측값, 가상 보장분석 엑셀 생성·재열기를 포함.
- 테스트는 가상 값만 사용하며 계정 비밀번호/고객 파일을 포함하지 않음.
- 실제 PC·모바일 브라우저 육안 검증은 미완료: 검증 환경의 브라우저 다운로드가 타임아웃/502 오류로 실패. 반응형 CSS가 실제 기기에서도 원하는 모양인지 테스트 배포 후 확인 필요.
- 모든 계산기의 모든 입력 조합이나 실제 보험사 원본 파일을 검증한 것은 아님. 계산 정확성 및 보험·세무 기준의 최신성 재검토는 이번 UI 개편 범위에 포함하지 않음.

재실행 명령(프로젝트 폴더에서):

```sh
python -m pip install -r requirements.txt
python -m unittest discover -s tests -v
python -m streamlit run app.py
```

## 테스트 서버 적용 순서

1. 기존 테스트 저장소/파일을 백업하고, 본 서버와 별개인 테스트 앱/브랜치인지 확인합니다.
2. 이 묶음의 파일을 테스트 저장소에 반영합니다. 숨김 경로인 `.streamlit/config.toml` 및 `assets` 폴더를 빠뜨리지 않습니다.
3. 기존 테스트 서버의 Secrets 설정은 유지합니다. `[passwords]` 아래의 Admin, Manager1, Basic, Crew, Dream 중 사용하는 계정 설정이 필요합니다. 비밀번호를 코드/ZIP/공개 저장소에 넣지 않습니다.
4. 실행 파일을 `app.py`로 유지하고 테스트 앱만 재시작합니다.
5. PC와 모바일에서 로그인 → 새 홈 → 검색 → 도구 진입 → 홈 복귀 → 로그아웃을 확인합니다. 모바일 메뉴 펼침/닫힘, 글자 겹침, 표 가로 스크롤도 확인합니다.
6. 가상 보장분석/실적 자료로 결과 다운로드 후 숫자와 엑셀 서식을 비교합니다. 문제가 있으면 백업한 테스트 버전으로 되돌립니다.

## 개인정보와 남은 범위

별도 고객 DB를 추가하지 않았다는 뜻이지, 자료가 서버에 전혀 전달되지 않는다는 뜻은 아닙니다. Streamlit 업로드·입력 자료는 서버에서 처리되며 기존 도구의 세션 상태와 메모리 캐시가 있습니다. 이번 작업은 기존 캐시 수명/삭제 정책을 전면 개편하지 않았습니다. 실제 개인정보를 넣기 전 별도의 데이터 처리 점검이 필요하며 테스트는 가상·비식별 자료로 진행하세요.

입력값의 페이지 간 보존, 개별 도구 전체 UI 개편, 추가 계산 기능, 회원가입/외부 고객 페이지/열람 추적은 이번 단계에 추가하지 않았습니다. 외부 서버나 요금제가 새로 필요하지 않지만 호스팅 서비스의 기존 이용 제한까지 없애는 것은 아닙니다.
