# Stage 6 · 교육·체크리스트 센터

작성일: 2026-09-23. Stage 5 전체 프로젝트 기준의 테스트용 배포 파일입니다. 실제 서버에는 반영하지 않았습니다.

## 완료 기능

- [x] 기존 16개 용어·3개 상담 시뮬레이션·3개 청구 퀴즈·5개 FAQ와 체크리스트 문구 보존
- [x] 교육 콘텐츠를 `data/education_content.json`으로 분리
- [x] 필수 필드·고유 ID·정답 범위·선택지·추천 도구 경로·기준 정보 검증
- [x] 용어 분류·다중 검색어 검색·현재 세션 즐겨찾기
- [x] 체크리스트 확인/미확인 수·메모·미리보기·TXT/PDF 출력
- [x] 첫 제출과 최근 제출 결과·제출 횟수·현재 세션 학습 진행률
- [x] 오답만 복습·답변 선택 초기화·해설 다시 보기
- [x] 답변 변경 시 다시 제출 안내, 문항 내용 변경 시 과거 기록을 진행률에서 제외
- [x] 연습 기록 초기화 확인/취소. 연구노트·체크리스트는 유지
- [x] 권한을 확인하는 추천 도구 이동, 고객자료 제작기 연결 추가
- [x] 수동 연구노트: 주제·자료명/출처/확인일·근거·미확인 사항·확인 계획·작성자 확인 상태
- [x] 학습자료·학습 기록·FAQ·연구노트의 검토 후 TXT/PDF 출력
- [x] 콘텐츠 편집일·출처·최신성 재검증 여부 분리 표시
- [ ] 실제 테스트 서버와 PC·모바일 브라우저 확인

콘텐츠 개수를 무작정 늘리는 단계가 아니라 기존 내용을 보존하면서 학습과 점검 흐름을 개선했습니다. 새 용어나 사례는 JSON에 추가할 수 있습니다. 문항 ID를 고유하게 유지하고 정답 번호는 0부터 입력합니다. 기존 문항 내용을 바꾸면 해당 문항의 이전 제출 기록은 현재 진행률에서 제외됩니다.

## 검증

전체 자동 검사 72개(기존 63개 + 신규 9개). 용어 검색·즐겨찾기·스키마 오류·문항 변경·재제출·오답 복습·초기화 취소/확인·출력 무효화·연구노트·세션 격리·권한별 이동·페이지 왕복을 확인했습니다. 기존 17개 페이지와 5개 역할의 회귀 검사를 포함합니다.

원본 Stage 5 코드의 교육 상수를 AST로 읽어 이관 JSON의 용어·체크리스트·시뮬레이션·퀴즈·FAQ 내용과 일치하는지 확인했습니다. 기존 12개 업무 모듈 해시 검사도 유지했습니다. 연구노트 예시 PDF는 이미지로 렌더링해 한글·줄바꿈·여백을 확인했습니다. 실제 PC·모바일 브라우저 확인은 미실시이며 AppTest가 이를 대체하지 않습니다.

## 콘텐츠와 개인정보 범위

기존 자체 작성 일반 교육 문구를 보존했습니다. 개별 법령·약관의 최신성은 이번 단계에서 재검증하지 않았으며 화면과 출력물에 이 상태를 표시합니다. 체크 완료는 법적 의무 이행이나 수료의 증명이 아닙니다.

연구노트의 출처 URL은 사용자가 기록하는 텍스트입니다. 외부 사이트를 자동 검색·열람·검증하지 않습니다. 작성자가 선택한 확인 상태도 시스템 검증 결과로 표시하지 않습니다.

입력·학습 기록·즐겨찾기는 현재 Streamlit 세션의 서버 메모리에서 처리합니다. 서버 파일·DB·공유 캐시에 사용자 입력을 저장하지 않습니다. 다운로드한 자료는 사용자 기기에 남습니다. 유료 API·DB·자동 발송·장기 고객이력은 추가하지 않았습니다. 실제 고객정보 대신 가상 사례로 사용하세요.

기존 `summer.py`의 직원 식별 명단은 유지되어 있으므로 전체 ZIP을 개인정보 없는 공개 배포본으로 취급하면 안 됩니다.

## 파일 변경

수정 4개: `modules/education_center.py`, `modules/app_registry.py`, `modules/session_store.py`, `README.md`.

신규 4개: `data/education_content.json`, `modules/education_content.py`, `tests/test_education_center.py`, 이 문서.

세션 복원에서는 제출·재도전·추천 도구 이동 버튼의 실행 상태를 초안으로 취급하지 않도록 보완했습니다. 새로운 외부 의존성은 없습니다.

## 테스트 서버 적용

1. 기존 테스트 프로젝트를 백업합니다.
2. ZIP 전체를 프로젝트 루트에 반영합니다. `data`와 `assets`를 포함하세요.
3. 기존 `.streamlit/secrets.toml`은 유지합니다.
4. `pip install -r requirements.txt` 후 `streamlit run app.py`로 실행합니다.
5. 교육센터에서 검색·오답 제출/복습·연구노트 작성·PDF 다운로드를 확인합니다.
6. 문제가 있으면 백업한 Stage 5 전체 파일로 복원하고 재시작합니다.

자동 검사: `python -m unittest discover -s tests -v`.

다음 7단계는 원수사 포털·공식 자료·업무 링크 개선입니다. 공식 정보의 링크 접속과 내용 검증은 구분해서 다룰 예정이며, 이번 단계에는 별도 공식 정보 수집을 추가하지 않았습니다.
