# 화랑 WORKSPACE · 8단계 기존 업무 UX

2026-09-23 / 7단계 전체 프로젝트를 포함한 테스트 서버 작업본. 자동 배포 없음.

## 이번에 직접 확인할 변화

보장분석·리모델링·적금/단기납·갱신/비갱신·상속세·청구 가이드·실손 비교·컨벤션·썸머·매니저 업적·수수료, 총 11개 업무의 제목 아래에 **업무 화면 개편 8단계** 영역이 추가됩니다. 원수사 포털은 7단계 개편을 유지합니다.

- 도구별 작업 순서와 준비사항을 첫 화면에 표시합니다.
- ‘준비사항·계산 기준 보기’ 버튼은 설명 팝업을 엽니다.
- ‘결과를 사용하기 전 확인할 항목’은 검토 목록과 기존 산식의 해석상 한계를 보여줍니다. 검토 완료를 자동 판정하지 않습니다.
- ‘관련 도구로 이동’은 현재 역할에 허용된 도구만 보여줍니다. 자료를 자동 복사하지 않습니다.
- 기존 공통 글자·버튼 크기와 모바일 줄바꿈 규칙을 적용합니다. 이번 버전은 홈 재설계나 개별 입력 화면 전체 재배치가 아닙니다.

## 업로드 검사 연결

보장분석 1곳, 청구 PDF 1곳, 컨벤션 1곳, 썸머 1곳, 매니저 업적 1곳, 수수료 3곳: 총 8곳에 기존 파일 검사기를 연결했습니다.

빈 파일·파일당 20MB 처리 한도·PDF 시그니처·XLSX 압축 구조/압축 해제량 등을 검사합니다. 실패하면 원문 내용·파일명을 오류 메시지로 노출하지 않고 실행을 중단합니다. 해당 실행에서 이전 파일의 결과가 아래에 계속 표시되지 않게 합니다. 선택 해제 자체가 모든 기존 분석 상태를 삭제한다는 의미는 아닙니다.

정상 파일은 객체와 읽기 위치를 유지한 채 기존 파서로 넘깁니다. 이 검사는 모든 손상·악성파일을 탐지하는 검사나 전체 세션 업로드 합산 한도 구현이 아닙니다. 업로드는 Streamlit 서버 메모리로 전달됩니다.

## 보존한 내용

기존 계산식·업무별 인정/제외 규칙·수수료 매칭·지급률·Excel/PDF 출력 구현을 변경하지 않았습니다. 기존 11개 업무의 원본 해시 검사에서 업로드 함수명 치환과 import 추가만 되돌린 뒤 원본과 비교합니다. 다른 변경은 검사 실패가 됩니다. 기존 원본 매니페스트는 변경하지 않았습니다.

직원 명단·보너스율도 그대로 유지했습니다. 새로운 DB, 유료 API, 고객 데이터 저장 기능, 파일 도구함을 추가하지 않았습니다.

## 남아 있는 점과 다음 감사 범위

- 직원 명단을 비밀 설정 등으로 분리하는 작업은 미실시입니다. 기존 명단을 제거하면 동일 운영이 깨질 수 있어 현재 데이터와 동작을 보존했습니다. 이 ZIP을 개인정보 없는 공개 배포본으로 취급하지 마세요.
- 상속세·실손·청구 규칙의 최신성 확인은 미완료입니다. 안내에 이 상태를 명시했으며 이번 UI 작업으로 최신 규칙을 검증했다고 표시하지 않습니다.
- 기존 12개 업무의 모든 계산 경계·실제 파일·출력 레이아웃 전수검사가 완료된 것은 아닙니다. 별도 체크리스트에 범위를 구분했습니다.
- 실제 PC/모바일 브라우저, 키보드·확대·인쇄 검사는 미실시입니다. AppTest는 실제 육안 검사를 대신하지 않습니다.
- 따라서 8단계 작업본 제공은 출시 승인이나 전체 설계 체크리스트의 완전 통과를 의미하지 않습니다. 다음 9단계 통합감사에서 잔여 항목을 이어갑니다.

## 적용 순서

1. 기존 테스트 저장소를 백업합니다.
2. ZIP 전체를 같은 프로젝트 경로에 반영합니다. 새 `modules/legacy_workflow.py`와 `modules/upload_ui.py`를 누락하지 마세요.
3. 기존 secrets 설정을 유지하고 테스트 서버를 재시작합니다.
4. 보장분석 또는 리모델링에서 ‘업무 화면 개편 8단계’ 표시와 도움말 팝업을 확인합니다.
5. 관련 도구 이동, 정상 합성 파일 및 빈 파일 업로드를 확인합니다. 실제 고객자료 대신 가상 자료로 먼저 점검하세요.
6. 되돌릴 때는 7단계 백업 전체를 복원하고 재시작합니다.

## 실행한 검사

- Streamlit 1.61.0 / Python 3.12 환경.
- `python -m unittest discover -s tests`: 86개 통과, 141.058초.
- 이후 추가한 `test_legacy_boundaries.py` 별도 실행: 4개 통과, 0.161초.
- 총 90개 고유 테스트 통과. 전체 90개를 한 번의 명령으로 실행한 결과는 아닙니다.
- 수정 9개·신규 6개, 이전 파일 삭제 없음. 브라우저 육안검사·운영 서버 배포 미실시.
